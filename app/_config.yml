title: I³ Dataset Index
description: >- # this means to ignore newlines until "baseurl:"
  Index of Innovation Datasets.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
plugins:
  - jekyll-feed

collections_dir: index
emptyArray: []

collections:
  datasets:
    output: true
  tools:
    output: true
  collections:
    output: true
    order:
      - intro.md
      - lens.md
      - publications.md
      - google_patents.md
      - nber.md

include:
  - _redirects
